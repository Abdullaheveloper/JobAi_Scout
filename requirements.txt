# ============================================================
# JobAI Scout — Python dependencies
# ============================================================
# The web app itself is Node.js/React (see package.json):
#   npm install
#
# This file installs the optional Python CV extractor service.
# Canonical list: services/cv-extractor/requirements.txt
#
# Install CV extractor deps:
#   pip install -r requirements.txt
#   # or:  pip install -r services/cv-extractor/requirements.txt
#
# Run:
#   npm run cv-extractor
#   # or:  cd services/cv-extractor && python main.py
#
# Optional (Project_plan/*.py doc generators only — not needed for the app):
#   pip install -r Project_plan/requirements.txt
# ============================================================

-r services/cv-extractor/requirements.txt
